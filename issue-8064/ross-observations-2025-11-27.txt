Ross’s Log.
---------------------------------------------------------------------------
Reminder notes. 
This is just to help me where I have arrived at so far.
It appears in my scenarios 3 events have occurred around the time of the stuck key
A modifier (shift) has been pressed
A backspace has been pressed 
Application focus has changed.
--------------------------------------------------------------------------


User Observation
System 3

Using microsft edge typing up a PR. Possibly change focus to check an excel and VScode while leaving the PR text for up

Test3 
Search for where the context is “THEN”  you can then see in backspaces the “HEN” but it appears the caps lock is toggled to resolve the shift being stuck. So I think it was stuck just before then.

IN Test3_others.xlsx index 477497 
This is just after a lower case line and Enter key pressed about to type the word “THEN” it appears only a shift was pressed and it could be stuck on. (or this was where it got stuck)

Maybe this wasn’t the spot. In the log it looks like I am pressing and releasing the shift key 

System 8
Saving Excel document when I noticed the shift key was stuck when I tried to type the number keys. Keymanapp_Windows_2025  Except I got the @)@%
The last two keys was the left shift, which did not clear the shift stuck, followed by the right shift which did free the key.
System 1
Corroupt log file. However, confirmed that using the physical keyboard pressing the left shift key would not release the stuck shift key but pressing the right Shift key did. 


Log Observations

First pass of the log things I noticed. 
ProcessHook: Key released: [‘?Shift’ 0x10]  221 time but the last one is line 489194 
However there is a ProcessHook: Key pressed: ['?Shift' 0x10]
After this at line 489214. Noting though these aren’t in pairs because there is 746 `Key pressed` and only 221 `Key released` log messages.

The focus for the current, which has the shift press, is removed while the key is still pressed
There is Backspace key right around this time. And I know from the comments that for backspace the event server does release/set of the modifier key. Does the combination of this focus loss mean the release is somehow lost in the process? Although I am not sure how I would have managed a backspace and right shift at the same time.

Looking at the SerialKeyEventServer::WndProc: hwnd=2040a msg=WM_KEYMAN_MODIFIER_EVENT wParam=a0 lParam=2a0002 m_ModifierKeyboardState=[LS:80 LC:0 LA:0 RS:80 RC:0 RA:0]
There is never a return to RS:0. Why
Look at lowLevelKeyboardProc we what to see an event like the following by for Right Shift. 
_kmnLowLevelKeyboardProc: wparam: 101  lparam: 19fd54 [vk:['?LShift' 0xa0] scan:2a flags:80 extra:0]
Via the log, I can confirm the last message of this type is a pressed and not released. Also that the extra: 4b4d0000  which means injected by keyman. There is no message after this received to say the key is released that is bit7 being set. 
This last one is line 489171.

With the SerialKeyEventServer 489176 and 489177 having two WM_KEYMAN_MODIFIER_EVENT calls the first RShift is “on” the second RShift is “off”

There is a Backspace key right around this time. 
So after the last right shift I am see a backspace. I see WM_KEYUP  for the backspace then WM_KILLFOCUS– Telling Keyman focus has changed. 


Something to look at as well 489136 flags of 11 indicate it was injected but the extra: is 0 so not by Keyman.

Notes for me. SerialKeyEventServer logs show the current state of the modifier not what it is about to be changed to.  

Found this comment in the code re R Shift verse L Shift
  “To add an additional wrinkle to an already wrinkly story, Keyman (ab)uses the scan code property
  of key events to identify key events that it has synthesized, as opposed to those generated by
  the user or by another app (e.g. Remote Desktop scenarios). Instead of using the correct scan
  code, it will stuff the key event with a 0xFF scan code, and replace that in its GetMessage
  so the end application gets a look at the correct scan code. However, this will obviously not
  work with Right Shift, which is differentiated from Left Shift _only_ by scan code. Fortunately,
  we can afford to not care about this -- it means a little extra flag setting behind the scenes
  as the right shift key is released and re-pressed during key events, but nothing too dramatic.”

before each letter. So either I did that or it is a clue. 
